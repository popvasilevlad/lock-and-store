name: CI Pipeline

on:
  pull_request:
    types: [opened, synchronize]
    
permissions:
  pull-requests: write

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
jobs:
  pr_opened:
    if: github.event.action == 'opened'
    name: Comment on PR opened
    runs-on: ubuntu-latest
    env:
      PR_URL: ${{ github.event.pull_request.html_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Add comment to PR
        run: gh pr comment "$PR_URL" --body "You have successfully opened a PR. Ensure you get at least one approval and all the tests pass before merging."

  pr_updated:
    if: github.event.action == 'synchronize'
    name: Comment on PR update
    runs-on: ubuntu-latest
    env:
       PR_URL: ${{ github.event.pull_request.html_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Add comment to PR
        run:  gh pr comment "$PR_URL" --body "You have successfully updated a PR. Please ensure again that you get at least one approval and all the tests pass before merging."
        
     
     
